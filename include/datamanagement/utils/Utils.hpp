#ifndef UTILS_HPP_
#define UTILS_HPP_

#include <Eigen/Eigen>

namespace datamanagement {
    inline Eigen::MatrixXd CSV2Eigen(std::string const &filepath) {
        // Read CSV file and convert it to Eigen matrix
        return Eigen::MatrixXd();
    }
} // namespace datamanagement

#endif